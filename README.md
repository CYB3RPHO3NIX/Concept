# Concept

Static UI concept showcase built with HTML, CSS, and JavaScript only.

## Files

- `index.html` - single-page UI gallery
- `styles.css` - layout and component styling
- `script.js` - lightweight interactions

## GitHub Pages

GitHub Pages is configured to publish directly from the `main` branch, so the site is served from the repository root without an Actions deployment workflow.
